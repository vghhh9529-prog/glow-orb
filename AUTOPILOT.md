# Glow Autopilot

عامل Glow Autopilot هو خدمة مراجعة مستقلة تعمل بدورة كل 20 دقيقة. في كل دورة يفحص حالة المستودع، يشغل `pnpm build`، يقرأ backlog التطوير، ويراجع صفحات المزايا العامة لبوتات Discord لإثراء الاقتراحات. يحفظ تقريراً JSON داخل `artifacts/autopilot` عند تشغيله محلياً أو داخل بيئة العامل.

## سياسة الأمان

العامل الحالي **قراءة وتحليل فقط**. لا يكتب في `src`، ولا ينفذ migrations، ولا يغير متغيرات الأسرار، ولا ينشئ commit، ولا ينفذ نشر إنتاجي. أي اقتراح ينتج من التحليل يجب أن يتحول إلى تغيير صغير مستقل مع اختبارات ومراجعة قبل الدمج.

## التشغيل

للفحص مرة واحدة استخدم `pnpm autopilot:once`. للتشغيل المستمر داخل خدمة مستقلة استخدم `pnpm autopilot`، حيث ينتظر العامل 20 دقيقة بين الدورات. صورة التشغيل الجاهزة موجودة في `Dockerfile.autopilot`.

## تفعيل الاقتراحات الذكية اختيارياً

لا يحتاج التدقيق الأساسي إلى مفتاح خارجي. يمكن تفعيل طبقة الاقتراحات الذكية في بيئة العامل فقط عبر `AUTOPILOT_ENABLE_LLM=true` و`OPENAI_API_KEY` و`AUTOPILOT_MODEL`. هذه الطبقة تقترح slices صغيرة ولا تكتب الكود ولا تدمج أي شيء. لا تضع هذه المتغيرات في المستودع أو في ملفات `.env` المرفوعة.

## دورة التوسع اللاحقة

بعد اعتماد كل slice، يضاف اختبار مستقل ثم يُراجع البناء. تبقى عمليات تغيير الأسرار، إضافة الصلاحيات، تشغيل SQL، إنشاء خدمة جديدة، والدمج أو النشر المباشر خلف بوابة أمان ولا ينفذها العامل تلقائياً.
